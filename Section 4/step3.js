/* 
BREAK IT DOWN
*/

/* 

-Explicitly write out the steps you need to take.
--Forces one to think more critically about their code and steps. Also helps find any lingering questions and misunderstandings.

*/

/*

Write a function which takes in a string and returns counts of each character in the string.

const charCount = (str) => {
    do something
    //return an object with keys that are lowercase aplhanumeric characters in the string.
}

const charCount = (str) => {
    //make object to return at end
    //loop over string
    //char number/letter && inside obj ? add one to count
    //char number/letter && not inside obj ? add it and set value to 1
    //else if anything that isnt alphanumeric, do nothing
    //build object and return 

}

*/
